#!/bin/sh

[ "$LUNA_AUTO_PAUSE" -eq 0 ] && exit

pactl subscribe | grep --line-buffered -o "'change' on card" | \
        xargs -I{} -n1 pactl list sinks  | \
        grep --line-buffered  -o 'Active Port:.*-speaker' | \
        xargs -I{} playerctl -a pause

