#!/bin/sh
if [ -z "$(pidof cmus)" ]
then
    screen -dmS 'cmus' /usr/bin/cmus
    screen -S 'cmus' -p 0 -X stuff ":clear^M:add $LUNA_MUSIC_DIR^M"
    screen -r -S 'cmus'
else
    screen -d cmus
    screen -r -S 'cmus'
fi
