#!/bin/sh

wttr_dir="$HOME/.cache/wttr"
mkdir -p "$wttr_dir"
/usr/bin/curl 'https://mausam.imd.gov.in/Satellite/Converted/IR1.gif' > "$wttr_dir/rad.gif" &
/usr/bin/curl 'wttr.in' > "$wttr_dir/chart" &
/usr/bin/curl 'wttr.in?format=v2' > "$wttr_dir/graph" &
wait
